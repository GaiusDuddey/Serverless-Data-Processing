# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
pytest-cov>=5.0.0

# AWS mocking for unit tests (no LocalStack needed for unit tests)
moto[sqs,dynamodb,s3,sns]>=5.0.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP requests (for integration tests / CSV uploader)
requests>=2.31.0

# Data handling
pandas>=2.2.0        # For CSV ingestion script

# Linting / formatting (optional but recommended)
black>=24.0.0
flake8>=7.0.0